namespace WinformsMVP.Common
{
    /// <summary>
    /// Screen corner that toast notifications appear in and stack from.
    /// Top corners stack downward; bottom corners stack upward.
    /// </summary>
    public enum ToastPosition
    {
        TopLeft,
        TopRight,
        BottomLeft,
        BottomRight
    }
}
