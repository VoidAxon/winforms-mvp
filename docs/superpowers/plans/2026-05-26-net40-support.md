# .NET Framework 4.0 サポート Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** WinformsMVP 主包从单一 `net48` 改为 multi-target `net40;net48`，让旧客户环境也能使用框架，且不破坏 net48 用户现有功能。

**Architecture:** 三个改造支柱 ——
1. `[CallerMemberName]` 用 internal polyfill attribute 解决（C# 编译器认名字，不依赖 BCL）
2. `IReadOnlyList<T>`（.NET 4.5+ 类型）改成 `ReadOnlyCollection<T>`（.NET 2.0 起就有）
3. `Microsoft.Extensions.Logging.Abstractions`（要求 net461+）替换为主包自家定义的 `WinformsMVP.Logging.ILogger` 抽象 + 独立适配器包 `WinformsMVP.Logging.MicrosoftExtensions`（仅 net48 用户用）

**Tech Stack:** C# `LangVersion=latest`、.NET Framework 4.0 / 4.8、xUnit 2.9.3（测试项目保持 net48，因为 xUnit 不支持 net40）。

---

## File Structure

### 新建文件
- `src/WinformsMVP/Polyfills/CallerInfoAttributes.cs` —— `[CallerMemberName]` 等 polyfill（仅 net40 编译）
- `src/WinformsMVP/Logging/LogLevel.cs` —— 日志级别枚举
- `src/WinformsMVP/Logging/ILogger.cs` —— 主接口（2 个方法）
- `src/WinformsMVP/Logging/ILoggerFactory.cs` —— 工厂接口
- `src/WinformsMVP/Logging/LoggerExtensions.cs` —— `LogInformation/LogError/...` 扩展方法
- `src/WinformsMVP/Logging/NullLogger.cs` —— 空实现（默认）
- `src/WinformsMVP/Logging/NullLoggerFactory.cs`
- `src/WinformsMVP/Logging/DebugLogger.cs` —— 用 `System.Diagnostics.Debug`
- `src/WinformsMVP/Logging/DebugLoggerFactory.cs`
- `src/WinformsMVP.Logging.MicrosoftExtensions/WinformsMVP.Logging.MicrosoftExtensions.csproj` —— 新适配器项目
- `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggerAdapter.cs`
- `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggerFactoryAdapter.cs`
- `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggingExtensions.cs` —— `.AsFrameworkLoggerFactory()`

### 修改文件
- `src/WinformsMVP/WinformsMVP.csproj` —— `TargetFramework` → `TargetFrameworks`，移除 M.E.L.Abstractions 依赖
- `src/WinformsMVP/Common/Validation/Core/ModelValidator.cs:44,179` —— `IReadOnlyList<>` → `ReadOnlyCollection<>`
- `src/WinformsMVP/Common/Validation/Extensions/ChangeTrackerValidationExtensions.cs:97` —— 同上
- `src/WinformsMVP/Services/IPlatformServices.cs` —— `ILoggerFactory` 切换命名空间
- `src/WinformsMVP/Services/Implementations/DefaultPlatformServices.cs` —— 同上
- `src/WinformsMVP/MVP/Presenters/PresenterBase.cs` —— 同上
- `src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs` —— 同上
- `src/WinformsMVP/Common/EventAggregator/EventAggregator.cs` —— 同上
- `src/WinformsMVP/winforms-mvp.sln` —— 添加新适配器项目
- `src/WinformsMVP.Samples/WinformsMVP.Samples.csproj` —— 添加适配器引用
- `src/WinformsMVP.Samples/LoggingDemoExample.cs` —— 改用 `WinformsMVP.Logging.ILogger`（扩展方法名兼容，调用零修改）
- `src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj` —— 同上
- `src/WinformsMVP.Samples.Tests/TestHelpers/CapturingLogger.cs` —— 重写为实现新 ILogger
- `src/WinformsMVP.Samples.Tests/Mocks/MockPlatformServices.cs`
- `src/WinformsMVP.Samples.Tests/Common/EventAggregatorLoggerTests.cs` —— 用新 LogLevel
- `src/WinformsMVP.Samples.Tests/ViewActions/ViewActionDispatcherHardeningTests.cs`
- `src/WinformsMVP.Samples.Tests/LoggingIntegrationTests.cs`
- `src/WinformsMVP.Samples.Tests/Services/DefaultPlatformServicesTests.cs`

---

## Phase 1: CallerMemberName polyfill + ReadOnlyCollection

低风险快速改动。先确认现有测试基线通过，再开工。

### Task 1.0: ベースライン確認

- [ ] **Step 1.0.1: 当前测试全通过基线**

Run: `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 全绿。若不通过，先回报状态、勿继续。

### Task 1.1: `IReadOnlyList<>` → `ReadOnlyCollection<>`

**Files:**
- Modify: `src/WinformsMVP/Common/Validation/Core/ModelValidator.cs:44`
- Modify: `src/WinformsMVP/Common/Validation/Core/ModelValidator.cs:179-205`
- Modify: `src/WinformsMVP/Common/Validation/Extensions/ChangeTrackerValidationExtensions.cs:97`
- Test: 既存测试全部通过

- [ ] **Step 1.1.1: 修改 `IModelValidator` 接口签名**

`src/WinformsMVP/Common/Validation/Core/ModelValidator.cs:1-5` 把命名空间 `System.Collections.Generic` 旁边加上 `System.Collections.ObjectModel`：

```csharp
using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Reflection;
```

第 44 行：

```csharp
// Before
IReadOnlyList<ValidationResult> ValidateAll(object model);

// After
ReadOnlyCollection<ValidationResult> ValidateAll(object model);
```

- [ ] **Step 1.1.2: 修改 `ModelValidatorImpl<T>.ValidateAll` 实现**

第 179-205 行：

```csharp
// Before
public IReadOnlyList<ValidationResult> ValidateAll(object model)
{
    // ... existing logic ...

    return standardResults.Select(r =>
    {
        var memberName = r.MemberNames.FirstOrDefault() ?? string.Empty;
        var order = _propertyOrders.TryGetValue(memberName, out var o) ? o : int.MaxValue;
        return new ValidationResult(r.ErrorMessage, r.MemberNames, order);
    })
    .OrderBy(r => r.Order)
    .ToList();
}

// After (改最后一行)
public ReadOnlyCollection<ValidationResult> ValidateAll(object model)
{
    // ... existing logic 完全保持 ...

    var ordered = standardResults.Select(r =>
    {
        var memberName = r.MemberNames.FirstOrDefault() ?? string.Empty;
        var order = _propertyOrders.TryGetValue(memberName, out var o) ? o : int.MaxValue;
        return new ValidationResult(r.ErrorMessage, r.MemberNames, order);
    })
    .OrderBy(r => r.Order)
    .ToList();

    return new ReadOnlyCollection<ValidationResult>(ordered);
}
```

- [ ] **Step 1.1.3: 修改扩展方法的 `out` 参数**

`src/WinformsMVP/Common/Validation/Extensions/ChangeTrackerValidationExtensions.cs`:
1. 第 1-5 行 using 添加 `System.Collections.ObjectModel`
2. 第 94-97 行：

```csharp
// Before
public static bool AcceptChangesIfValid<T>(
    this ChangeTracker<T> tracker,
    IModelValidator validator,
    out IReadOnlyList<ValidationResult> errors) where T : class, ICloneable

// After
public static bool AcceptChangesIfValid<T>(
    this ChangeTracker<T> tracker,
    IModelValidator validator,
    out ReadOnlyCollection<ValidationResult> errors) where T : class, ICloneable
```

实现内的 `errors = validator.ValidateAll(...)` 一行**不变**（返回类型已经匹配）。

- [ ] **Step 1.1.4: 跑测试**

Run: `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 全通过（调用方都用 LINQ，不依赖 IReadOnlyList 特性）。

### Task 1.2: CallerMemberName polyfill 准备

> 这一步先把 polyfill 文件**加进来但不参与编译**。Phase 3 切换到 multi-target 时再让它在 net40 编译期参与。当下不会改变任何编译产物。

**Files:**
- Create: `src/WinformsMVP/Polyfills/CallerInfoAttributes.cs`
- Modify: `src/WinformsMVP/WinformsMVP.csproj`（确认 `<Compile Remove="obj\**" />` 之外，polyfill 默认是会被 SDK 自动包含的）

- [ ] **Step 1.2.1: 创建 polyfill 文件**

Create `src/WinformsMVP/Polyfills/CallerInfoAttributes.cs`:

```csharp
// Polyfill for [CallerMemberName] / [CallerFilePath] / [CallerLineNumber]
// on .NET Framework 4.0 (these attributes shipped in BCL starting with 4.5).
//
// The C# 5+ compiler recognises these attributes BY NAME and NAMESPACE,
// regardless of which assembly defines them and regardless of accessibility.
// Declaring them as `internal` keeps them out of the public API surface and
// avoids CS0436 collisions for net48+ consumers whose BCL already defines
// the real attributes.

#if NET40
namespace System.Runtime.CompilerServices
{
    [System.AttributeUsage(System.AttributeTargets.Parameter, Inherited = false)]
    internal sealed class CallerMemberNameAttribute : System.Attribute { }

    [System.AttributeUsage(System.AttributeTargets.Parameter, Inherited = false)]
    internal sealed class CallerFilePathAttribute : System.Attribute { }

    [System.AttributeUsage(System.AttributeTargets.Parameter, Inherited = false)]
    internal sealed class CallerLineNumberAttribute : System.Attribute { }
}
#endif
```

`#if NET40` 块在当前 `net48` 单 target 下**完全不会编译**，所以这一步对现状零影响。Phase 3 把项目切到 multi-target 时它会在 net40 编译期自动生效。

- [ ] **Step 1.2.2: 跑测试确认零影响**

Run: `dotnet build src\WinformsMVP\WinformsMVP.csproj -c Debug`
Expected: 编译成功，0 warnings, 0 errors（polyfill 内容因 `#if NET40` 不参与编译）。

Run: `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 全通过。

### Task 1.3: Phase 1 commit

- [ ] **Step 1.3.1: 提交**

```bash
git add src/WinformsMVP/Polyfills/CallerInfoAttributes.cs
git add src/WinformsMVP/Common/Validation/Core/ModelValidator.cs
git add src/WinformsMVP/Common/Validation/Extensions/ChangeTrackerValidationExtensions.cs
git commit -m "$(cat <<'EOF'
refactor: Prepare for .NET 4.0 support (polyfill + ReadOnlyCollection)

- Add `[CallerMemberName]` / `[CallerFilePath]` / `[CallerLineNumber]`
  polyfills under #if NET40 (no-op on current net48 single-target).
- Replace `IReadOnlyList<ValidationResult>` (BCL 4.5+) with
  `ReadOnlyCollection<ValidationResult>` (BCL 2.0+) in IModelValidator
  and ChangeTrackerValidationExtensions.

No behavioural change. Callers use LINQ only and are unaffected.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Run: `git status`
Expected: working tree clean (除了 `.claude/settings.local.json`)

---

## Phase 2: Logger 抽象 + 适配器包

最大的一步。分四个子阶段：定义抽象 → 迁移框架内部 → 创建适配器包 → 迁移样例/测试。

### Task 2.1: 定义 `WinformsMVP.Logging` 抽象

**Files:**
- Create: `src/WinformsMVP/Logging/LogLevel.cs`
- Create: `src/WinformsMVP/Logging/ILogger.cs`
- Create: `src/WinformsMVP/Logging/ILoggerFactory.cs`
- Create: `src/WinformsMVP/Logging/LoggerExtensions.cs`
- Create: `src/WinformsMVP/Logging/NullLogger.cs`
- Create: `src/WinformsMVP/Logging/NullLoggerFactory.cs`
- Create: `src/WinformsMVP/Logging/DebugLogger.cs`
- Create: `src/WinformsMVP/Logging/DebugLoggerFactory.cs`

- [ ] **Step 2.1.1: 创建 LogLevel.cs**

Create `src/WinformsMVP/Logging/LogLevel.cs`:

```csharp
namespace WinformsMVP.Logging
{
    /// <summary>
    /// Log severity levels. Values match Microsoft.Extensions.Logging.LogLevel for
    /// straightforward mapping by the optional adapter package.
    /// </summary>
    public enum LogLevel
    {
        Trace = 0,
        Debug = 1,
        Information = 2,
        Warning = 3,
        Error = 4,
        Critical = 5,
        None = 6,
    }
}
```

- [ ] **Step 2.1.2: 创建 ILogger.cs**

Create `src/WinformsMVP/Logging/ILogger.cs`:

```csharp
using System;

namespace WinformsMVP.Logging
{
    /// <summary>
    /// Minimal logging abstraction the framework depends on. The host application can
    /// implement this directly, use a built-in implementation (NullLogger / DebugLogger),
    /// or bridge Microsoft.Extensions.Logging via the
    /// <c>WinformsMVP.Logging.MicrosoftExtensions</c> adapter package.
    /// </summary>
    public interface ILogger
    {
        bool IsEnabled(LogLevel level);

        /// <summary>
        /// Emits a single log record. <paramref name="message"/> uses
        /// <see cref="string.Format(string, object[])"/> placeholder syntax (e.g. "{0}").
        /// Implementations may capture <paramref name="args"/> as structured data or just
        /// format them into a string.
        /// </summary>
        void Log(LogLevel level, Exception exception, string message, params object[] args);
    }
}
```

- [ ] **Step 2.1.3: 创建 ILoggerFactory.cs**

Create `src/WinformsMVP/Logging/ILoggerFactory.cs`:

```csharp
using System;

namespace WinformsMVP.Logging
{
    /// <summary>
    /// Creates <see cref="ILogger"/> instances scoped by category. Hosts typically
    /// register one factory at startup and pass it to <c>DefaultPlatformServices</c>.
    /// </summary>
    public interface ILoggerFactory
    {
        ILogger CreateLogger(string categoryName);
        ILogger CreateLogger(Type type);
    }
}
```

- [ ] **Step 2.1.4: 创建 LoggerExtensions.cs（API 兼容关键点）**

Create `src/WinformsMVP/Logging/LoggerExtensions.cs`:

```csharp
using System;

namespace WinformsMVP.Logging
{
    /// <summary>
    /// Convenience extension methods. Names match
    /// <c>Microsoft.Extensions.Logging.LoggerExtensions</c> so call sites do not
    /// change when migrating between abstractions.
    /// </summary>
    public static class LoggerExtensions
    {
        public static void LogTrace(this ILogger logger, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Trace, null, message, args);
        }

        public static void LogDebug(this ILogger logger, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Debug, null, message, args);
        }

        public static void LogInformation(this ILogger logger, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Information, null, message, args);
        }

        public static void LogWarning(this ILogger logger, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Warning, null, message, args);
        }

        public static void LogError(this ILogger logger, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Error, null, message, args);
        }

        public static void LogError(this ILogger logger, Exception exception, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Error, exception, message, args);
        }

        public static void LogCritical(this ILogger logger, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Critical, null, message, args);
        }

        public static void LogCritical(this ILogger logger, Exception exception, string message, params object[] args)
        {
            if (logger == null) return;
            logger.Log(LogLevel.Critical, exception, message, args);
        }
    }
}
```

- [ ] **Step 2.1.5: 创建 NullLogger.cs + NullLoggerFactory.cs**

Create `src/WinformsMVP/Logging/NullLogger.cs`:

```csharp
using System;

namespace WinformsMVP.Logging
{
    /// <summary>
    /// Sink that discards all log records. Used as the default when no factory is
    /// configured, and as a safe non-null fallback throughout the framework.
    /// </summary>
    public sealed class NullLogger : ILogger
    {
        public static readonly NullLogger Instance = new NullLogger();

        private NullLogger() { }

        public bool IsEnabled(LogLevel level) => false;

        public void Log(LogLevel level, Exception exception, string message, params object[] args)
        {
            // intentionally empty
        }
    }
}
```

Create `src/WinformsMVP/Logging/NullLoggerFactory.cs`:

```csharp
using System;

namespace WinformsMVP.Logging
{
    /// <summary>
    /// Factory that always returns <see cref="NullLogger.Instance"/>.
    /// </summary>
    public sealed class NullLoggerFactory : ILoggerFactory
    {
        public static readonly NullLoggerFactory Instance = new NullLoggerFactory();

        private NullLoggerFactory() { }

        public ILogger CreateLogger(string categoryName) => NullLogger.Instance;
        public ILogger CreateLogger(Type type) => NullLogger.Instance;
    }
}
```

- [ ] **Step 2.1.6: 创建 DebugLogger.cs + DebugLoggerFactory.cs**

Create `src/WinformsMVP/Logging/DebugLogger.cs`:

```csharp
using System;
using System.Diagnostics;

namespace WinformsMVP.Logging
{
    /// <summary>
    /// Simple logger that writes to <see cref="Debug.WriteLine(string)"/>. Useful for
    /// .NET Framework 4.0 hosts that cannot reference Microsoft.Extensions.Logging
    /// but still want visibility into framework internals while debugging.
    /// </summary>
    public sealed class DebugLogger : ILogger
    {
        private readonly string _category;

        public DebugLogger(string category)
        {
            _category = category ?? string.Empty;
        }

        public bool IsEnabled(LogLevel level) => level != LogLevel.None;

        public void Log(LogLevel level, Exception exception, string message, params object[] args)
        {
            if (!IsEnabled(level)) return;

            string formatted;
            if (args != null && args.Length > 0)
            {
                try { formatted = string.Format(message ?? string.Empty, args); }
                catch (FormatException) { formatted = message ?? string.Empty; }
            }
            else
            {
                formatted = message ?? string.Empty;
            }

            Debug.WriteLine("[" + level + "] [" + _category + "] " + formatted);
            if (exception != null) Debug.WriteLine(exception);
        }
    }
}
```

Create `src/WinformsMVP/Logging/DebugLoggerFactory.cs`:

```csharp
using System;

namespace WinformsMVP.Logging
{
    public sealed class DebugLoggerFactory : ILoggerFactory
    {
        public ILogger CreateLogger(string categoryName) => new DebugLogger(categoryName);
        public ILogger CreateLogger(Type type) => new DebugLogger(type?.FullName);
    }
}
```

- [ ] **Step 2.1.7: 构建确认（抽象层独立可编译）**

Run: `dotnet build src\WinformsMVP\WinformsMVP.csproj -c Debug`
Expected: 成功（此时主包还在引用 M.E.L，但新的 Logging 命名空间不冲突）。

### Task 2.2: 迁移框架内部 4 个文件

> 关键技巧：因为 `LogInformation`/`LogError` 都是扩展方法，*调用代码不变*。只需要改 using 语句、字段类型、方法签名。

**Files:**
- Modify: `src/WinformsMVP/Services/IPlatformServices.cs`
- Modify: `src/WinformsMVP/Services/Implementations/DefaultPlatformServices.cs`
- Modify: `src/WinformsMVP/MVP/Presenters/PresenterBase.cs`
- Modify: `src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs`
- Modify: `src/WinformsMVP/Common/EventAggregator/EventAggregator.cs`

- [ ] **Step 2.2.1: 修改 IPlatformServices.cs**

`src/WinformsMVP/Services/IPlatformServices.cs` 全文替换为：

```csharp
using WinformsMVP.Logging;

namespace WinformsMVP.Services
{
    /// <summary>
    /// Interface for platform services.
    /// Provides infrastructure services such as messages, dialogs, logging, and file operations.
    /// </summary>
    public interface IPlatformServices
    {
        /// <summary>
        /// Dialog provider (file open, save, folder selection, etc.)
        /// </summary>
        IDialogProvider DialogProvider { get; }

        /// <summary>
        /// Message service (message boxes, toast notifications, etc.)
        /// </summary>
        IMessageService MessageService { get; }

        /// <summary>
        /// File service (file read/write, directory operations, etc.)
        /// </summary>
        IFileService FileService { get; }

        /// <summary>
        /// Window navigator (modal/non-modal window display)
        /// </summary>
        IWindowNavigator WindowNavigator { get; }

        /// <summary>
        /// Logger factory for creating loggers.
        /// Defaults to <see cref="NullLoggerFactory.Instance"/> when not configured.
        /// Bridge Microsoft.Extensions.Logging or other ecosystems via the
        /// <c>WinformsMVP.Logging.MicrosoftExtensions</c> adapter package.
        /// </summary>
        ILoggerFactory LoggerFactory { get; }
    }
}
```

- [ ] **Step 2.2.2: 修改 DefaultPlatformServices.cs**

`src/WinformsMVP/Services/Implementations/DefaultPlatformServices.cs` —— 全文替换 `using Microsoft.Extensions.Logging;` 和 `using Microsoft.Extensions.Logging.Abstractions;` 两行为：

```csharp
using WinformsMVP.Logging;
```

`<remarks>` / `<example>` 注释里所有 `Microsoft.Extensions.Logging.*` 引用更新为 `WinformsMVP.Logging.*` —— 注意 `<example>` 块需要给一个新示例（避免误导用户该处再用 M.E.L 直接）：

将 18-27 行的 `<example>` 块替换为：

```csharp
    /// <example>
    /// <code>
    /// // Host opts in to the Debug provider (no extra dependencies):
    /// PlatformServices.Default = new DefaultPlatformServices(
    ///     viewMappingRegister: register,
    ///     loggerFactory: new DebugLoggerFactory());
    ///
    /// // Or bridge Microsoft.Extensions.Logging (requires the adapter package):
    /// var msFactory = LoggerFactory.Create(b => b.AddDebug());
    /// PlatformServices.Default = new DefaultPlatformServices(
    ///     viewMappingRegister: register,
    ///     loggerFactory: msFactory.AsFrameworkLoggerFactory());
    /// </code>
    /// </example>
```

类型保持原结构（`Lazy<ILoggerFactory> _loggerFactory`），但 `ILoggerFactory` 现在指向新命名空间。第 104 行：

```csharp
// Before
_loggerFactory = new Lazy<ILoggerFactory>(() => loggerFactory ?? NullLoggerFactory.Instance);

// After (一字不差 —— NullLoggerFactory.Instance 现在指向新命名空间)
_loggerFactory = new Lazy<ILoggerFactory>(() => loggerFactory ?? NullLoggerFactory.Instance);
```

- [ ] **Step 2.2.3: 修改 PresenterBase.cs**

`src/WinformsMVP/MVP/Presenters/PresenterBase.cs:2` —— `using Microsoft.Extensions.Logging;` 替换为：

```csharp
using WinformsMVP.Logging;
```

第 79-104 行的 `ILogger Logger` 实现需要适应新工厂签名。原代码：

```csharp
private ILogger _logger;

protected ILogger Logger
{
    get
    {
        if (_logger == null)
        {
            var loggerFactory = Platform.LoggerFactory;
            _logger = loggerFactory.CreateLogger(this.GetType());
        }
        return _logger;
    }
}
```

**不变**。因为 `ILoggerFactory.CreateLogger(Type)` 在新接口里也存在，签名相同。仅 `using` 改变。

- [ ] **Step 2.2.4: 修改 ViewActionDispatcher.cs**

`src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs:3-4` 两行 `using Microsoft.Extensions.Logging...` 替换为：

```csharp
using WinformsMVP.Logging;
```

第 34 行：

```csharp
// Before
private ILogger _logger = NullLogger.Instance;
// After: 完全相同（NullLogger.Instance 现在是新命名空间下的单例）
private ILogger _logger = NullLogger.Instance;
```

第 46-50 行的属性、第 134/147/162/181 行的 `_logger.LogXxx(...)` 调用全部**不变**。

- [ ] **Step 2.2.5: 修改 EventAggregator.cs**

`src/WinformsMVP/Common/EventAggregator/EventAggregator.cs:7-8` 两行 `using Microsoft.Extensions.Logging...` 替换为：

```csharp
using WinformsMVP.Logging;
```

字段、构造函数、`LogError` 调用全部**不变**。

- [ ] **Step 2.2.6: 移除主包对 M.E.L 的依赖**

`src/WinformsMVP/WinformsMVP.csproj` 第 19-27 行的 `<ItemGroup>` 整块替换为：

```xml
  <!--
    Framework now defines its own minimal logging abstraction in WinformsMVP.Logging.
    Host applications that want to use Microsoft.Extensions.Logging or any other
    ecosystem reference the WinformsMVP.Logging.MicrosoftExtensions adapter package
    (or write their own ILogger implementation). The main package itself has zero
    external dependencies so it can be multi-targeted to net40.
  -->
```

- [ ] **Step 2.2.7: 编译主包并跑测试**

Run: `dotnet build src\WinformsMVP\WinformsMVP.csproj -c Debug`
Expected: 成功，0 errors.

预期此时**测试项目和样例项目**会编译失败（因为它们的 LoggerFactory 还是 M.E.L 类型）。**先不跑测试**，下面 Task 2.3、2.4 修复后再跑。

### Task 2.3: 创建适配器包 `WinformsMVP.Logging.MicrosoftExtensions`

**Files:**
- Create: `src/WinformsMVP.Logging.MicrosoftExtensions/WinformsMVP.Logging.MicrosoftExtensions.csproj`
- Create: `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggerAdapter.cs`
- Create: `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggerFactoryAdapter.cs`
- Create: `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggingExtensions.cs`
- Modify: `src/winforms-mvp.sln` —— 添加新项目

- [ ] **Step 2.3.1: 创建项目文件**

Create `src/WinformsMVP.Logging.MicrosoftExtensions/WinformsMVP.Logging.MicrosoftExtensions.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net462</TargetFramework>
    <OutputType>Library</OutputType>
    <RootNamespace>WinformsMVP.Logging.MicrosoftExtensions</RootNamespace>
    <AssemblyName>WinformsMVP.Logging.MicrosoftExtensions</AssemblyName>
    <LangVersion>latest</LangVersion>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <!--
      Adapter package: bridges WinformsMVP.Logging.ILogger and
      Microsoft.Extensions.Logging.ILogger. Only host applications that opt in to
      Microsoft's ecosystem need to take this dependency.
    -->
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\WinformsMVP\WinformsMVP.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Compile Remove="obj\**" />
    <EmbeddedResource Remove="obj\**" />
    <None Remove="obj\**" />
  </ItemGroup>

</Project>
```

- [ ] **Step 2.3.2: 创建 MicrosoftLoggerAdapter.cs**

Create `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggerAdapter.cs`:

```csharp
using System;
using Ms = Microsoft.Extensions.Logging;
using Fx = WinformsMVP.Logging;

namespace WinformsMVP.Logging.MicrosoftExtensions
{
    /// <summary>
    /// Adapts a <see cref="Ms.ILogger"/> instance so it can be consumed wherever the
    /// framework expects a <see cref="Fx.ILogger"/>. Forwarding preserves
    /// <c>Microsoft.Extensions.Logging</c>'s formatter pipeline, so structured
    /// properties, scopes, and providers all work as configured upstream.
    /// </summary>
    public sealed class MicrosoftLoggerAdapter : Fx.ILogger
    {
        private readonly Ms.ILogger _inner;

        public MicrosoftLoggerAdapter(Ms.ILogger inner)
        {
            _inner = inner ?? throw new ArgumentNullException(nameof(inner));
        }

        public bool IsEnabled(Fx.LogLevel level) => _inner.IsEnabled(MapLevel(level));

        public void Log(Fx.LogLevel level, Exception exception, string message, params object[] args)
        {
            // Microsoft.Extensions.Logging's ILogger.Log<TState>(...) extension methods
            // (LogInformation, LogError, ...) ultimately call the same overload; reusing
            // the extension keeps message-template parsing and structured property
            // capture identical to native M.E.L. callers.
            var msLevel = MapLevel(level);
            if (exception == null)
            {
                Ms.LoggerExtensions.Log(_inner, msLevel, message, args);
            }
            else
            {
                Ms.LoggerExtensions.Log(_inner, msLevel, exception, message, args);
            }
        }

        internal static Ms.LogLevel MapLevel(Fx.LogLevel level)
        {
            switch (level)
            {
                case Fx.LogLevel.Trace: return Ms.LogLevel.Trace;
                case Fx.LogLevel.Debug: return Ms.LogLevel.Debug;
                case Fx.LogLevel.Information: return Ms.LogLevel.Information;
                case Fx.LogLevel.Warning: return Ms.LogLevel.Warning;
                case Fx.LogLevel.Error: return Ms.LogLevel.Error;
                case Fx.LogLevel.Critical: return Ms.LogLevel.Critical;
                case Fx.LogLevel.None: return Ms.LogLevel.None;
                default: return Ms.LogLevel.Information;
            }
        }
    }
}
```

- [ ] **Step 2.3.3: 创建 MicrosoftLoggerFactoryAdapter.cs**

Create `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggerFactoryAdapter.cs`:

```csharp
using System;
using Ms = Microsoft.Extensions.Logging;
using Fx = WinformsMVP.Logging;

namespace WinformsMVP.Logging.MicrosoftExtensions
{
    /// <summary>
    /// Adapts a <see cref="Ms.ILoggerFactory"/> so it can be passed to
    /// <c>DefaultPlatformServices</c> as a <see cref="Fx.ILoggerFactory"/>.
    /// </summary>
    public sealed class MicrosoftLoggerFactoryAdapter : Fx.ILoggerFactory
    {
        private readonly Ms.ILoggerFactory _inner;

        public MicrosoftLoggerFactoryAdapter(Ms.ILoggerFactory inner)
        {
            _inner = inner ?? throw new ArgumentNullException(nameof(inner));
        }

        public Fx.ILogger CreateLogger(string categoryName)
            => new MicrosoftLoggerAdapter(_inner.CreateLogger(categoryName));

        public Fx.ILogger CreateLogger(Type type)
            => new MicrosoftLoggerAdapter(_inner.CreateLogger(type?.FullName ?? string.Empty));
    }
}
```

- [ ] **Step 2.3.4: 创建 MicrosoftLoggingExtensions.cs**

Create `src/WinformsMVP.Logging.MicrosoftExtensions/MicrosoftLoggingExtensions.cs`:

```csharp
using Ms = Microsoft.Extensions.Logging;
using Fx = WinformsMVP.Logging;

namespace WinformsMVP.Logging.MicrosoftExtensions
{
    public static class MicrosoftLoggingExtensions
    {
        /// <summary>
        /// Wraps a Microsoft.Extensions.Logging factory so it satisfies the
        /// framework's <see cref="Fx.ILoggerFactory"/> contract. Use this at the
        /// composition root when configuring <c>DefaultPlatformServices</c>.
        /// </summary>
        public static Fx.ILoggerFactory AsFrameworkLoggerFactory(this Ms.ILoggerFactory factory)
            => new MicrosoftLoggerFactoryAdapter(factory);
    }
}
```

- [ ] **Step 2.3.5: 把新项目加入 solution**

Run:
```powershell
dotnet sln src\winforms-mvp.sln add src\WinformsMVP.Logging.MicrosoftExtensions\WinformsMVP.Logging.MicrosoftExtensions.csproj
```

Expected: `Project ... added to the solution.`

- [ ] **Step 2.3.6: 构建适配器包**

Run: `dotnet build src\WinformsMVP.Logging.MicrosoftExtensions\WinformsMVP.Logging.MicrosoftExtensions.csproj -c Debug`
Expected: 成功，0 errors.

### Task 2.4: 迁移样例 + 测试项目

最繁琐的一步：每个文件都要从 `Microsoft.Extensions.Logging` 改用 `WinformsMVP.Logging`。但因为扩展方法名一致，主要是改 using 和 mock 类型。

**Files:**
- Modify: `src/WinformsMVP.Samples/WinformsMVP.Samples.csproj`
- Modify: `src/WinformsMVP.Samples/LoggingDemoExample.cs`
- Modify: `src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj`
- Modify: `src/WinformsMVP.Samples.Tests/TestHelpers/CapturingLogger.cs`
- Modify: `src/WinformsMVP.Samples.Tests/Mocks/MockPlatformServices.cs`
- Modify: `src/WinformsMVP.Samples.Tests/Common/EventAggregatorLoggerTests.cs`
- Modify: `src/WinformsMVP.Samples.Tests/ViewActions/ViewActionDispatcherHardeningTests.cs`
- Modify: `src/WinformsMVP.Samples.Tests/LoggingIntegrationTests.cs`
- Modify: `src/WinformsMVP.Samples.Tests/Services/DefaultPlatformServicesTests.cs`

- [ ] **Step 2.4.1: 重写 CapturingLogger.cs**

Replace `src/WinformsMVP.Samples.Tests/TestHelpers/CapturingLogger.cs` 全文：

```csharp
using System;
using System.Collections.Generic;
using WinformsMVP.Logging;

namespace WinformsMVP.Samples.Tests.TestHelpers
{
    /// <summary>
    /// In-memory <see cref="ILogger"/> for tests that need to assert on log output.
    /// Records every log call into <see cref="Entries"/> with level, formatted message, and exception.
    /// </summary>
    public class CapturingLogger : ILogger
    {
        public List<LogEntry> Entries { get; } = new List<LogEntry>();

        public bool IsEnabled(LogLevel level) => true;

        public void Log(LogLevel level, Exception exception, string message, params object[] args)
        {
            string formatted;
            if (args != null && args.Length > 0)
            {
                try { formatted = string.Format(message ?? string.Empty, args); }
                catch (FormatException) { formatted = message ?? string.Empty; }
            }
            else
            {
                formatted = message ?? string.Empty;
            }

            Entries.Add(new LogEntry
            {
                Level = level,
                Message = formatted,
                Exception = exception
            });
        }
    }

    /// <summary>
    /// Factory that hands out the same <see cref="CapturingLogger"/> regardless of category name.
    /// Lets callers inspect a single log stream across multiple components.
    /// </summary>
    public class CapturingLoggerFactory : ILoggerFactory
    {
        public CapturingLogger Logger { get; } = new CapturingLogger();

        public ILogger CreateLogger(string categoryName) => Logger;
        public ILogger CreateLogger(Type type) => Logger;
    }

    public class LogEntry
    {
        public LogLevel Level { get; set; }
        public string Message { get; set; }
        public Exception Exception { get; set; }
    }
}
```

- [ ] **Step 2.4.2: 修改 MockPlatformServices.cs**

Replace `src/WinformsMVP.Samples.Tests/Mocks/MockPlatformServices.cs` 全文：

```csharp
using WinformsMVP.Logging;
using WinformsMVP.Services;

namespace WinformsMVP.Samples.Tests.Mocks
{
    /// <summary>
    /// Mock implementation of platform services for testing.
    /// Provides access to all mock services.
    /// </summary>
    public class MockPlatformServices : IPlatformServices
    {
        public MockPlatformServices()
        {
            MessageService = new MockMessageService();
            DialogProvider = new MockDialogProvider();
            FileService = new MockFileService();
            WindowNavigator = new MockWindowNavigator();

            // Use NullLoggerFactory for tests (no logging output, zero performance overhead)
            LoggerFactory = NullLoggerFactory.Instance;
        }

        // Expose as concrete types (for test verification)
        public MockMessageService MessageService { get; }
        public MockDialogProvider DialogProvider { get; }
        public MockFileService FileService { get; }
        public MockWindowNavigator WindowNavigator { get; }

        // Interface implementation (returns the same instances)
        IMessageService IPlatformServices.MessageService => MessageService;
        IDialogProvider IPlatformServices.DialogProvider => DialogProvider;
        IFileService IPlatformServices.FileService => FileService;
        IWindowNavigator IPlatformServices.WindowNavigator => WindowNavigator;

        /// <summary>
        /// Logger factory for tests.
        /// Default: NullLoggerFactory (no output).
        /// Can be replaced with custom factory for logging verification tests.
        /// </summary>
        public ILoggerFactory LoggerFactory { get; set; }

        /// <summary>
        /// Resets all mock states
        /// </summary>
        public void Reset()
        {
            MessageService.Clear();
            FileService.Clear();
        }
    }
}
```

- [ ] **Step 2.4.3: 修改 EventAggregatorLoggerTests.cs**

`src/WinformsMVP.Samples.Tests/Common/EventAggregatorLoggerTests.cs` 第 4 行：

```csharp
// Before
using Microsoft.Extensions.Logging;

// After
using WinformsMVP.Logging;
```

其余代码（`LogLevel.Error` 等枚举值用法）保持不变。

- [ ] **Step 2.4.4: 修改 ViewActionDispatcherHardeningTests.cs**

`src/WinformsMVP.Samples.Tests/ViewActions/ViewActionDispatcherHardeningTests.cs` 第 3 行：

```csharp
// Before
using Microsoft.Extensions.Logging;

// After
using WinformsMVP.Logging;
```

- [ ] **Step 2.4.5: 修改 DefaultPlatformServicesTests.cs**

`src/WinformsMVP.Samples.Tests/Services/DefaultPlatformServicesTests.cs` 第 3-4 行：

```csharp
// Before
using Microsoft.Extensions.Logging;
using Microsoft.Extensions.Logging.Abstractions;

// After
using WinformsMVP.Logging;
```

其余 (`NullLoggerFactory.Instance`、`platform.LoggerFactory`) 保持不变。

- [ ] **Step 2.4.6: 修改 LoggingIntegrationTests.cs**

`src/WinformsMVP.Samples.Tests/LoggingIntegrationTests.cs` 第 4-5 行：

```csharp
// Before
using Microsoft.Extensions.Logging;
using Microsoft.Extensions.Logging.Abstractions;

// After
using WinformsMVP.Logging;
```

第 28-75 行的 `MockLogger` 类实现了 M.E.L 风格的 `Log<TState>(...)` 签名，需要改成新接口：

```csharp
// Before
private class MockLogger : ILogger
{
    public List<LogEntry> LoggedMessages { get; } = new List<LogEntry>();

    public IDisposable BeginScope<TState>(TState state) => null;

    public bool IsEnabled(LogLevel logLevel) => true;

    public void Log<TState>(
        LogLevel logLevel,
        EventId eventId,
        TState state,
        Exception exception,
        Func<TState, Exception, string> formatter)
    {
        LoggedMessages.Add(new LogEntry
        {
            LogLevel = logLevel,
            Message = formatter(state, exception),
            Exception = exception,
            State = state
        });
    }
}

private class LogEntry
{
    public LogLevel LogLevel { get; set; }
    public string Message { get; set; }
    public Exception Exception { get; set; }
    public object State { get; set; }
}

private class MockLoggerFactory : ILoggerFactory
{
    private readonly MockLogger _logger = new MockLogger();

    public MockLogger Logger => _logger;

    public void AddProvider(ILoggerProvider provider) { }

    public ILogger CreateLogger(string categoryName) => _logger;

    public void Dispose() { }
}

// After
private class MockLogger : ILogger
{
    public List<LogEntry> LoggedMessages { get; } = new List<LogEntry>();

    public bool IsEnabled(LogLevel level) => true;

    public void Log(LogLevel level, Exception exception, string message, params object[] args)
    {
        string formatted;
        if (args != null && args.Length > 0)
        {
            try { formatted = string.Format(message ?? string.Empty, args); }
            catch (FormatException) { formatted = message ?? string.Empty; }
        }
        else
        {
            formatted = message ?? string.Empty;
        }

        LoggedMessages.Add(new LogEntry
        {
            LogLevel = level,
            Message = formatted,
            Exception = exception
        });
    }
}

private class LogEntry
{
    public LogLevel LogLevel { get; set; }
    public string Message { get; set; }
    public Exception Exception { get; set; }
}

private class MockLoggerFactory : ILoggerFactory
{
    private readonly MockLogger _logger = new MockLogger();

    public MockLogger Logger => _logger;

    public ILogger CreateLogger(string categoryName) => _logger;
    public ILogger CreateLogger(Type type) => _logger;
}
```

注意 `State` 字段被删除（旧 M.E.L 接口的 generic state 在新抽象中没有等价物，且测试没断言这字段）。检查 240-243 行的 `Assert.IsType<NullLoggerFactory>(mockServices.LoggerFactory);` —— 仍然有效（新命名空间下的 NullLoggerFactory）。

- [ ] **Step 2.4.7: 修改 LoggingDemoExample.cs**

`src/WinformsMVP.Samples/LoggingDemoExample.cs` 文件开头任何 `using Microsoft.Extensions.Logging;` 替换为：

```csharp
using WinformsMVP.Logging;
```

预期类似 `Logger.LogInformation(...)` 调用全部不变（扩展方法名一致）。读全文确认是否有用 `EventId`、`BeginScope` 等 M.E.L 特有 API；如果有，简化为我们的抽象（删 EventId、删 scope）。

Run: `dotnet build src\WinformsMVP.Samples\WinformsMVP.Samples.csproj -c Debug`
检查 errors，按提示修复 LoggingDemoExample.cs。

- [ ] **Step 2.4.8: 修改 Samples.csproj 移除 M.E.L 直接依赖**

`src/WinformsMVP.Samples/WinformsMVP.Samples.csproj` 19-23 行的 PackageReference 块：

```xml
<!-- Before -->
<ItemGroup>
  <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="8.0.0" />
  <!-- Concrete logger providers belong to the host app, not the framework library. -->
  <PackageReference Include="Microsoft.Extensions.Logging" Version="8.0.0" />
  <PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="8.0.0" />
</ItemGroup>
```

如果 LoggingDemoExample.cs 不再使用 M.E.L 直接 API（理论上不应该 —— 全是通过 PresenterBase.Logger 间接用），就可以删除 `Microsoft.Extensions.Logging` 和 `Microsoft.Extensions.Logging.Debug` 两行；保留 `Microsoft.Extensions.DependencyInjection`（DI demo 用得到）。

如果样例需要演示 M.E.L 集成，添加适配器项目引用：

```xml
<ItemGroup>
  <ProjectReference Include="..\WinformsMVP\WinformsMVP.csproj" />
  <ProjectReference Include="..\WinformsMVP.Logging.MicrosoftExtensions\WinformsMVP.Logging.MicrosoftExtensions.csproj" />
</ItemGroup>
```

- [ ] **Step 2.4.9: 修改 Tests.csproj 移除 M.E.L 直接依赖**

`src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj` 第 21 行：

```xml
<!-- Before -->
<PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.0" />

<!-- After: 删除这行；新 ILogger 抽象由主包提供 -->
```

- [ ] **Step 2.4.10: 全量构建 + 测试**

Run: `dotnet build src\winforms-mvp.sln -c Debug`
Expected: 全部项目编译成功，0 errors.

Run: `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 所有测试通过。

### Task 2.5: Phase 2 commit

- [ ] **Step 2.5.1: 提交**

```bash
git add src/WinformsMVP/Logging/
git add src/WinformsMVP/Services/IPlatformServices.cs
git add src/WinformsMVP/Services/Implementations/DefaultPlatformServices.cs
git add src/WinformsMVP/MVP/Presenters/PresenterBase.cs
git add src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs
git add src/WinformsMVP/Common/EventAggregator/EventAggregator.cs
git add src/WinformsMVP/WinformsMVP.csproj
git add src/WinformsMVP.Logging.MicrosoftExtensions/
git add src/winforms-mvp.sln
git add src/WinformsMVP.Samples/
git add src/WinformsMVP.Samples.Tests/

git commit -m "$(cat <<'EOF'
refactor: Replace M.E.L dependency with WinformsMVP.Logging abstraction

Main package no longer depends on Microsoft.Extensions.Logging.Abstractions
(which requires net461+). Introduces a minimal WinformsMVP.Logging.ILogger
contract with extension methods named identically to M.E.L (LogInformation,
LogError, ...), so call sites do not change.

Host applications that want Microsoft.Extensions.Logging bridge it via the
new WinformsMVP.Logging.MicrosoftExtensions adapter package:

    factory.AsFrameworkLoggerFactory()

Built-in implementations:
- NullLogger / NullLoggerFactory (default, silent)
- DebugLogger / DebugLoggerFactory (System.Diagnostics.Debug, BCL-only)

This change is a prerequisite for multi-targeting net40, where the M.E.L
abstractions cannot be referenced.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Run: `git status`
Expected: working tree clean (except `.claude/settings.local.json`).

---

## Phase 3: Multi-target `net40;net48` + 验证

### Task 3.1: 切换主包到 multi-target

**Files:**
- Modify: `src/WinformsMVP/WinformsMVP.csproj`

- [ ] **Step 3.1.1: 修改 TargetFramework**

`src/WinformsMVP/WinformsMVP.csproj` 第 4 行：

```xml
<!-- Before -->
<TargetFramework>net48</TargetFramework>

<!-- After -->
<TargetFrameworks>net40;net48</TargetFrameworks>
```

- [ ] **Step 3.1.2: 构建主包（双 target）**

Run: `dotnet build src\WinformsMVP\WinformsMVP.csproj -c Debug`

Expected:
- net48 编译成功
- net40 可能因为 `IReadOnlyList<T>` 残留、`is not` C# 9 patterns 或其他遗漏报错。若如此，按错误修复。

**已知可能问题及修复:**

1. **`message is not T typedMessage` 这种 C# 9 模式** —— EventAggregator.cs:346 用了。C# 9 模式编译期生成，运行时不需要 BCL 支持，所以 net40 应该 OK（但如果报错，改成 `!(message is T typedMessage)`）。

2. **Tuple 解构、`(a, b) = ...`** —— 检查代码是否用了。如果用了，需要引入 `System.ValueTuple` NuGet 包，或重写。

3. **`?.` null-conditional / `??=` null-coalescing assignment** —— `?.` 是 C# 6（编译期），`??=` 是 C# 8（也是编译期）。两者都 OK。

4. **`init` setters** —— 未在主包中使用，但若 grep 发现需要 polyfill `IsExternalInit`。

- [ ] **Step 3.1.3: 检查输出**

Run: `ls src\WinformsMVP\bin\Debug\`

Expected: 看到 `net40\WinformsMVP.dll` 和 `net48\WinformsMVP.dll` 两个子目录。

- [ ] **Step 3.1.4: 跑测试（net48 path）**

Run: `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 所有测试通过。

> 测试项目仍是单 target net48（xUnit 不支持 net40），但它引用的 WinformsMVP 是 multi-target dll。dotnet 默认会选 net48 的 dll，确认这一点。

### Task 3.2: Phase 3 commit

- [ ] **Step 3.2.1: 提交**

```bash
git add src/WinformsMVP/WinformsMVP.csproj
git commit -m "$(cat <<'EOF'
build: Multi-target WinformsMVP to net40;net48

Main package now produces both a net40 and a net48 assembly. Test project
stays on net48 because xUnit does not support net40. Legacy host
applications stuck on .NET Framework 4.0 can now reference the package.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Run: `git status`
Expected: working tree clean.

### Task 3.3: 最终验证

- [ ] **Step 3.3.1: 完整解决方案构建**

Run: `dotnet build src\winforms-mvp.sln -c Debug`
Expected: 全部项目成功。

Run: `dotnet build src\winforms-mvp.sln -c Release`
Expected: Release 配置也成功（暴露任何 Debug-only 隐藏的问题）。

- [ ] **Step 3.3.2: 完整测试套件**

Run: `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj`
Expected: 全绿。

- [ ] **Step 3.3.3: 确认 net40 dll 真的能被 net40 工程引用（独立验证）**

可选：创建一个临时 .NET 4.0 控制台项目引用 net40 dll，验证基本类型可访问、polyfill 正确生效。若时间紧可跳过。

---

## Self-Review Checklist

执行前自查，避免回头：

- [x] **Spec 覆盖** —— 三个阻碍点（CallerMemberName / IReadOnlyList / M.E.L）都有对应任务。
- [x] **没有 placeholder** —— 每个 step 都有完整代码或精确命令。
- [x] **类型一致性** —— `WinformsMVP.Logging.ILogger` / `ILoggerFactory` / `LogLevel` / `NullLogger` 在主包、适配器、测试三处签名一致。
- [x] **现有测试预期** —— 测试代码用 `LogLevel.Error/Warning/Debug` 字面量；新 enum 字面量名称完全匹配 M.E.L，零修改。
- [x] **依赖方向** —— 主包零外部依赖；适配器单向依赖主包 + M.E.L；测试单向依赖主包；样例可选引用适配器。

---

## 风险与回滚

**最高风险点：net40 编译失败**（Task 3.1.2）。常见原因：
- 某处用了 4.5+ BCL 类型（`Task.CompletedTask`、`Func<...>` arity > 16、`HashCode`...）
- 某处用了运行时需要 polyfill 的 C# 特性

**回滚策略：** Phase 1/2/3 是独立 commit。net40 编译失败可单独回滚 Phase 3，保留 Phase 1+2 的改动（它们对 net48 用户也有价值：去掉了 M.E.L 强制依赖）。

```bash
git revert HEAD                 # 只回滚 Phase 3
# OR
git reset --hard <Phase 2 commit hash>
```

---

## 实施完成定义

下列条件**全部满足**即视为完成：

1. ✅ `dotnet build src\winforms-mvp.sln -c Release` 全部项目编译成功
2. ✅ `dotnet test src\WinformsMVP.Samples.Tests\WinformsMVP.Samples.Tests.csproj` 全绿
3. ✅ `src\WinformsMVP\bin\Release\net40\WinformsMVP.dll` 存在且体积合理
4. ✅ `src\WinformsMVP\bin\Release\net48\WinformsMVP.dll` 存在
5. ✅ `git log --oneline -3` 看到三个新 commit
6. ✅ `git status` 工作树清洁

---

## Phase 3 修订（2026-05-26）

### 背景

Phase 3 首次 dispatch 时，net40 build 暴露了 plan 初版没预见的 **10 个编译错误**，归为两类问题。原 Task 3.1 步骤无效，本节作为 Task 3.1 的**正式替代**。

### 新发现的问题

**问题 A — `EventHandler<TEventArgs>` 约束差异**：.NET Framework 4.0 中泛型 `EventHandler<TEventArgs>` 声明带有 `where TEventArgs : EventArgs` 约束；.NET 4.5 才放宽。框架中有 4 处违反：

1. [src/WinformsMVP/MVP/ViewActions/IViewActionBinder.cs:30](src/WinformsMVP/MVP/ViewActions/IViewActionBinder.cs#L30) — `event EventHandler<ActionRequestEventArgs> ActionTriggered` — 因为 [ActionRequestEventArgs](src/WinformsMVP/Common/Events/ActionRequestEventArgs.cs#L5) 当前不继承 `EventArgs`
2. [src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs:75](src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs#L75) — 同上
3. [src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs:55](src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs#L55) — `event EventHandler<ViewAction> ActionExecuted` — `ViewAction` 是 struct，无法继承 `EventArgs`
4. [src/WinformsMVP/Common/Events/ExecutionResult.cs:25](src/WinformsMVP/Common/Events/ExecutionResult.cs#L25) — `event EventHandler<TResult> Completed` — 无约束泛型

**问题 B — `System.ValueTuple<,>` 缺失**：value tuple 基础设施在 .NET Framework 4.7 才进 BCL。3 处用法：

- [src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs:43-46](src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs#L43) — 内部 `_bindingStrategies` 和 `_activeSubscriptions` 字段
- [src/WinformsMVP/MVP/ViewActions/ViewActionBinderExtensions.cs:28](src/WinformsMVP/MVP/ViewActions/ViewActionBinderExtensions.cs#L28) — **public API** `params (ViewAction action, Component component)[]`

### 修复方案

#### Task 3.0: 修复 4 处 `EventHandler<T>` 违规

##### Step 3.0.1: `ActionRequestEventArgs` 继承 `EventArgs`

`src/WinformsMVP/Common/Events/ActionRequestEventArgs.cs` 第 1-12 行：

```csharp
using System;
using WinformsMVP.MVP.ViewActions;

namespace WinformsMVP.Common.Events
{
    public class ActionRequestEventArgs : EventArgs
    {
        public ActionRequestEventArgs(ViewAction actionKey)
        {
            ActionKey = actionKey;
        }
        public ViewAction ActionKey { get; }
    }

    public class ActionRequestEventArgs<T> : ActionRequestEventArgs, IActionRequestEventArgsWithValue
    {
        public ActionRequestEventArgs(ViewAction actionKey, T value) : base(actionKey)
        {
            Value = value;
        }

        public T Value { get; }
        public object GetValue() => Value;
    }
}
```

**改动**：第 1 行加 `using System;`，第 5 行加 `: EventArgs`。`ActionRequestEventArgs<T>` 通过继承链自动获得 EventArgs 血统。**完全向后兼容**。

##### Step 3.0.2: 新增 `ActionExecutedEventArgs`

Create `src/WinformsMVP/Common/Events/ActionExecutedEventArgs.cs`:

```csharp
using System;
using WinformsMVP.MVP.ViewActions;

namespace WinformsMVP.Common.Events
{
    /// <summary>
    /// EventArgs carrying the <see cref="ViewAction"/> key for the
    /// <see cref="ViewActionDispatcher.ActionExecuted"/> event. Wrapping in
    /// EventArgs is required because <see cref="ViewAction"/> is a struct and
    /// .NET Framework 4.0's <c>EventHandler&lt;T&gt;</c> requires
    /// <c>T : EventArgs</c>.
    /// </summary>
    public class ActionExecutedEventArgs : EventArgs
    {
        public ActionExecutedEventArgs(ViewAction actionKey)
        {
            ActionKey = actionKey;
        }

        public ViewAction ActionKey { get; }
    }
}
```

##### Step 3.0.3: `ViewActionDispatcher.ActionExecuted` 签名变更

`src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs`:

- 第 1-4 行 using 区添加 `using WinformsMVP.Common.Events;`
- 第 55 行：
  ```csharp
  // Before
  public event EventHandler<ViewAction> ActionExecuted;
  // After
  public event EventHandler<ActionExecutedEventArgs> ActionExecuted;
  ```
- 第 170 行的触发处：
  ```csharp
  // Before
  ActionExecuted?.Invoke(this, actionKey);
  // After
  ActionExecuted?.Invoke(this, new ActionExecutedEventArgs(actionKey));
  ```

##### Step 3.0.4: `ViewActionBinder.OnActionExecuted` 签名变更

`src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs:234-237`:

```csharp
// Before
private void OnActionExecuted(object sender, ViewAction actionKey)
{
    UpdateCanExecuteStates();
}
// After
private void OnActionExecuted(object sender, ActionExecutedEventArgs e)
{
    UpdateCanExecuteStates();
}
```

文件顶部 using 区已经有 `using WinformsMVP.Common.Events;`（第 6 行），无需新加。

订阅代码（`_dispatcher.ActionExecuted += OnActionExecuted;` 第 224 行）不变 —— 委托类型自动适配。

##### Step 3.0.5: 新增 `ExecutionCompletedEventArgs<TResult>`

Create `src/WinformsMVP/Common/Events/ExecutionCompletedEventArgs.cs`:

```csharp
using System;

namespace WinformsMVP.Common.Events
{
    /// <summary>
    /// EventArgs carrying the result for the
    /// <see cref="ExecutionResult{TResult}.Completed"/> event. Wrapping in
    /// EventArgs is required because <typeparamref name="TResult"/> is
    /// unconstrained and .NET Framework 4.0's <c>EventHandler&lt;T&gt;</c>
    /// requires <c>T : EventArgs</c>.
    /// </summary>
    public class ExecutionCompletedEventArgs<TResult> : EventArgs
    {
        public ExecutionCompletedEventArgs(TResult result)
        {
            Result = result;
        }

        public TResult Result { get; }
    }
}
```

##### Step 3.0.6: `ExecutionResult<TResult>.Completed` 签名变更

`src/WinformsMVP/Common/Events/ExecutionResult.cs`:

- 第 1-3 行 using 区已有 `using System;`，无变化
- 第 25 行：
  ```csharp
  // Before
  public event EventHandler<TResult> Completed;
  // After
  public event EventHandler<ExecutionCompletedEventArgs<TResult>> Completed;
  ```
- 第 74 行 `SetResult` 触发处：
  ```csharp
  // Before
  Completed?.Invoke(this, result);
  // After
  Completed?.Invoke(this, new ExecutionCompletedEventArgs<TResult>(result));
  ```

**实际订阅者影响**：grep 全仓库 0 个 `.Completed +=` 订阅，仅是预防性 API 改动。

##### Step 3.0.7: 编译验证（仍 net48 单 target）

Run: `dotnet build src/WinformsMVP/WinformsMVP.csproj -c Debug`
Expected: 0 errors. Step 3.0.1-3.0.6 不改变运行时行为，只换 event payload 类型。

Run: `dotnet test src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 所有测试通过（modulo 已知 flaky concurrency tests）。

##### Step 3.0.8: 测试代码影响检查

- [src/WinformsMVP.Samples.Tests/ViewActions/ViewActionDispatcherHardeningTests.cs:54](src/WinformsMVP.Samples.Tests/ViewActions/ViewActionDispatcherHardeningTests.cs#L54) 和第 225 行：`dispatcher.ActionExecuted += (s, k) => raised = true;`
- lambda 参数 `k` 类型自动从 `ViewAction` 变成 `ActionExecutedEventArgs`
- 两个测试都不读 `k` 内容，**只触发 bool flag** —— 编译通过，无需改动

Run: `dotnet test ... --filter "FullyQualifiedName~ViewActionDispatcherHardeningTests"` 单独验证一次。

#### Task 3.0bis: 修复 net40 反射 / DataAnnotations BCL 差异

第二次 Phase 3 尝试在 multi-target 切换后暴露了 8 个额外编译错误，归为 4 类。修复都是 net48-safe（更老的 API overload 在 net48 也可用），不需要 `#if NET40` 分支。

##### Step 3.0bis.1: `ValidationContext` 构造函数（2 处）

`src/WinformsMVP/Common/Validation/Core/ModelValidator.cs` 第 190 行和第 258 行（行号以 Phase 3 修订前的状态计）：

```csharp
// Before (两处)
var context = new System.ComponentModel.DataAnnotations.ValidationContext(model);
// L258 变体
var context = new System.ComponentModel.DataAnnotations.ValidationContext(typedModel);

// After (两处，显式传 null 给后两个参数)
var context = new System.ComponentModel.DataAnnotations.ValidationContext(model, serviceProvider: null, items: null);
// L258
var context = new System.ComponentModel.DataAnnotations.ValidationContext(typedModel, serviceProvider: null, items: null);
```

##### Step 3.0bis.2: `PropertyInfo.GetValue` 单参重载（5 处合计）

net40 只有 `GetValue(object obj, object[] index)`，没有 `GetValue(object obj)`（后者是 net45 加的方便重载）。修改方式：传 `null` 给 index。

文件 1：`src/WinformsMVP/Common/Validation/Core/ModelValidator.cs` 第 262 行：

```csharp
// Before
var propertyValue = rule.PropertyInfo.GetValue(typedModel);

// After
var propertyValue = rule.PropertyInfo.GetValue(typedModel, null);
```

文件 2：`src/WinformsMVP/Common/Extensions/ControlBindingExtensions.cs` 第 164, 173, 184 行：

```csharp
// Before (3 处类似)
var value = propInfo.GetValue(viewModel);

// After
var value = propInfo.GetValue(viewModel, null);
```

##### Step 3.0bis.3: `PropertyInfo.SetValue` 双参重载（1 处）

`src/WinformsMVP/Common/Extensions/ControlBindingExtensions.cs` 第 175 行：

```csharp
// Before
propInfo.SetValue(viewModel, value);

// After
propInfo.SetValue(viewModel, value, null);
```

##### Step 3.0bis.4: `PropertyInfo.GetCustomAttribute<T>` 泛型扩展（1 处）

`System.Reflection.CustomAttributeExtensions.GetCustomAttribute<T>` 在 .NET 4.5 才加入。net40 只有非泛型的 `GetCustomAttributes(Type, bool)` 实例方法。

`src/WinformsMVP/Common/Validation/Core/ModelValidator.cs` 第 327 行：

```csharp
// Before
var orderAttr = prop.GetCustomAttribute<ValidationOrderAttribute>(inherit: true);

// After
var orderAttr = prop
    .GetCustomAttributes(typeof(ValidationOrderAttribute), inherit: true)
    .Cast<ValidationOrderAttribute>()
    .FirstOrDefault();
```

确认 `System.Linq` 已在文件 using 区（[ModelValidator.cs:5](src/WinformsMVP/Common/Validation/Core/ModelValidator.cs#L5) 已有 `using System.Linq;`）。

##### Step 3.0bis.5: 编译验证（net48 单 target）

切回 `<TargetFramework>net48</TargetFramework>`（暂时撤销 Phase B 的 csproj 改动）然后跑：

```
dotnet build src/WinformsMVP/WinformsMVP.csproj -c Debug
dotnet test src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj -v minimal
```

Expected: 0 errors, 所有测试通过（modulo flaky concurrency tests）。这一步验证 Step 3.0bis.1-4 在 net48 path 下是无副作用的。

##### Step 3.0bis.6: 重新启用 multi-target，再次验证

恢复 csproj 为 `<TargetFrameworks>net40;net48</TargetFrameworks>`（带 System.ValueTuple 条件 PackageReference）：

```
dotnet build src/WinformsMVP/WinformsMVP.csproj -c Debug
```

Expected: net40 和 net48 双 target 均 0 errors. 若仍出现新的 BCL 错误，**STOP report BLOCKED**（不再自动修复 —— 需要决策是否继续投资 net40）。

#### Task 3.1（修订）: 重写 `ViewActionBinder` 内部 ValueTuple + 添加 `System.ValueTuple` NuGet

##### Step 3.1.1: 重写 `ViewActionBinder` 内部 tuple 字段

`src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs` 第 43-46 行的私有字段使用 value tuple。重写为具名 struct。

在 `ViewActionBinder` 类内（**或同文件命名空间内**），添加两个私有 nested struct：

```csharp
private struct BindingStrategy
{
    public Action<Component, Delegate> Attacher;
    public Action<Component, Delegate> Detacher;
}

private struct ActiveSubscription
{
    public Component Control;
    public Delegate Handler;
    public Action<Component, Delegate> Detacher;
}
```

替换 43-46 行的字段声明为：

```csharp
private readonly Dictionary<Type, BindingStrategy> _bindingStrategies
    = new Dictionary<Type, BindingStrategy>();
private readonly List<ActiveSubscription> _activeSubscriptions
    = new List<ActiveSubscription>();
```

更新 [RegisterStrategy 第 110-113 行](src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs#L110)：

```csharp
public void RegisterStrategy<T>(Action<T, Delegate> attacher, Action<T, Delegate> detacher) where T : Component
{
    _bindingStrategies[typeof(T)] = new BindingStrategy
    {
        Attacher = (comp, handler) => attacher((T)comp, handler),
        Detacher = (comp, handler) => detacher((T)comp, handler)
    };
}
```

更新 [ApplyBindingStrategy 第 297-311 行](src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs#L297):

```csharp
private void ApplyBindingStrategy(Component control, Delegate handler)
{
    Type currentType = control.GetType();
    while (currentType != null && currentType != typeof(object))
    {
        if (_bindingStrategies.TryGetValue(currentType, out var strategy))
        {
            Delegate specificHandler = handler;
            strategy.Attacher(control, specificHandler);
            _activeSubscriptions.Add(new ActiveSubscription
            {
                Control = control,
                Handler = specificHandler,
                Detacher = strategy.Detacher
            });
            return;
        }
        currentType = currentType.BaseType;
    }
}
```

更新 [Unbind 第 290-294 行](src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs#L290) 的 tuple deconstruction:

```csharp
// Before
foreach (var (control, handler, detacher) in _activeSubscriptions)
{
    detacher(control, handler);
}
// After
foreach (var sub in _activeSubscriptions)
{
    sub.Detacher(sub.Control, sub.Handler);
}
```

##### Step 3.1.2: 为 `ViewActionBinderExtensions` 保留 ValueTuple，加 NuGet 包

`ViewActionBinderExtensions.AddRange(params (ViewAction, Component)[])` 是 **public API**，是约定俗成的 ValueTuple 调用方式。强行重写会破坏用户调用代码。改用 NuGet 包仅 net40 引入。

`src/WinformsMVP/WinformsMVP.csproj` 在 multi-target 切换后，加入条件 PackageReference。完整 csproj 此时大约长这样（注意 `TargetFrameworks` 改为复数 + 新的 ItemGroup）：

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net40;net48</TargetFrameworks>
    <OutputType>Library</OutputType>
    <RootNamespace>WinformsMVP</RootNamespace>
    <AssemblyName>WinformsMVP</AssemblyName>
    <LangVersion>latest</LangVersion>
    <UseWindowsForms>true</UseWindowsForms>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="System.Windows.Forms" />
    <Reference Include="System.Drawing" />
    <Reference Include="System.ComponentModel.DataAnnotations" />
  </ItemGroup>

  <!--
    System.ValueTuple is BCL on net47+, but missing on net40-net46. We expose
    ValueTuple in public API (ViewActionBinderExtensions.AddRange(params (...)[])),
    so net40 consumers need the NuGet package. net48 ignores this reference.
  -->
  <ItemGroup Condition="'$(TargetFramework)' == 'net40'">
    <PackageReference Include="System.ValueTuple" Version="4.5.0" />
  </ItemGroup>

  <!--
    Framework now defines its own minimal logging abstraction in WinformsMVP.Logging.
    Host applications that want to use Microsoft.Extensions.Logging or any other
    ecosystem reference the WinformsMVP.Logging.MicrosoftExtensions adapter package
    (or write their own ILogger implementation). The main package itself has zero
    external dependencies (apart from System.ValueTuple on net40) so it can be
    multi-targeted to net40.
  -->

  <ItemGroup>
    <Compile Remove="obj\**" />
    <EmbeddedResource Remove="obj\**" />
    <None Remove="obj\**" />
  </ItemGroup>

</Project>
```

##### Step 3.1.3: 编译双 target

Run: `dotnet build src/WinformsMVP/WinformsMVP.csproj -c Debug`
Expected: net40 和 net48 双 target 均 0 errors.

如果还出现新错误（C# 9 patterns、其他 BCL 缺失等），按 plan 原版 Task 3.1.2 "已知可能问题及修复" 处理。如果遇到未知错误，STOP report BLOCKED.

Run: `ls src/WinformsMVP/bin/Debug/`
Expected: 看到 `net40/WinformsMVP.dll` 和 `net48/WinformsMVP.dll`.

##### Step 3.1.4: 跑测试（net48 path）

Run: `dotnet test src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj -v minimal`
Expected: 全部测试通过（modulo flaky concurrency tests）。

#### Task 3.2（修订）: 单 commit 合并 Task 3.0 + Task 3.1

```bash
git add src/WinformsMVP/Common/Events/ActionRequestEventArgs.cs
git add src/WinformsMVP/Common/Events/ActionExecutedEventArgs.cs
git add src/WinformsMVP/Common/Events/ExecutionResult.cs
git add src/WinformsMVP/Common/Events/ExecutionCompletedEventArgs.cs
git add src/WinformsMVP/MVP/ViewActions/ViewActionDispatcher.cs
git add src/WinformsMVP/MVP/ViewActions/ViewActionBinder.cs
git add src/WinformsMVP/WinformsMVP.csproj

git commit -m "$(cat <<'EOF'
build: Multi-target WinformsMVP to net40;net48

Main package now produces both a net40 and a net48 assembly. To meet net40's
stricter `EventHandler<TEventArgs : EventArgs>` constraint:

- ActionRequestEventArgs now inherits System.EventArgs
- ViewActionDispatcher.ActionExecuted carries a new ActionExecutedEventArgs
  wrapper (the previous EventHandler<ViewAction> violates net40 because
  ViewAction is a struct)
- ExecutionResult<TResult>.Completed carries a new
  ExecutionCompletedEventArgs<TResult> wrapper (the previous EventHandler<TResult>
  is unconstrained and violates net40)

ViewActionBinder's private value-tuple fields are rewritten as named structs.
ViewActionBinderExtensions.AddRange still exposes a value-tuple public API;
net40 consumers get System.ValueTuple via a conditional PackageReference.

Test project stays on net48 because xUnit does not support net40. Legacy host
applications stuck on .NET Framework 4.0 can now reference the package.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

#### Task 3.3 不变（最终验证）

按原 plan 的 Task 3.3 执行 Debug + Release 全量构建 + 测试 + DLL 存在性检查。

### 修订后的实施完成定义

1. ✅ `dotnet build src/winforms-mvp.sln -c Release` 全部项目编译成功
2. ✅ `dotnet test src/WinformsMVP.Samples.Tests/WinformsMVP.Samples.Tests.csproj` 全绿（modulo 已知 flaky）
3. ✅ `src/WinformsMVP/bin/Release/net40/WinformsMVP.dll` 存在
4. ✅ `src/WinformsMVP/bin/Release/net48/WinformsMVP.dll` 存在
5. ✅ `git log --oneline -3` 看到三个 commit
6. ✅ `git status` 工作树清洁
7. ✅ `ActionRequestEventArgs` 继承 `EventArgs`，新增 `ActionExecutedEventArgs` 和 `ExecutionCompletedEventArgs<T>` 各一个
8. ✅ `ViewActionBinder` 内部不再使用 ValueTuple；`ViewActionBinderExtensions` 保留 ValueTuple 公开 API
9. ✅ `WinformsMVP.csproj` 含 `<PackageReference Include="System.ValueTuple" Condition="'$(TargetFramework)' == 'net40'" />`
